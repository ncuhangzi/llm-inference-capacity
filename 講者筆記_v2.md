# LLM 推論與服務容量 · 第二版 — 講者筆記

共 91 頁。資料查核日期 2026-09-08。

## 導讀

### 1. LLM 推論與服務容量

開場先問這五個問題，等於宣告這份報告的邊界：我們談的是單一 request 的機制與
整機容量的算法，不談訓練、不談模型品質評測。

第一版報告與這一版最大的差別：所有數字都對齊實際要上線的量化版本
（122B 用 NVFP4、27B 用 FP8），並且把 speculative decoding 從一頁擴充成完整一章。

建議節奏：導讀 3 分鐘，CH1–CH2 共 15 分鐘，CH3–CH4 架構 15 分鐘，CH5 量化 8 分鐘，
CH6 記憶體 10 分鐘，CH7 speculative 12 分鐘，CH8–CH9 量測 15 分鐘，結論 5 分鐘。
時間不夠時可以整章跳過 CH1（假設聽眾已懂 Transformer），附錄留給 Q&A。

### 2. 怎麼讀這份報告
*先講清楚顏色、名詞與數字的可信度分級*

這一頁是防呆用的。第一版最容易被誤讀的地方，就是聽眾把示意曲線當成實測。
這一版把可信度分級講明，每張教學示例圖上也都留了紅字。

顏色系統也統一了：整份報告只要看到橘色就是「頻寬 / KV」，藍色就是「算力 / 權重」，
粉紅就是 speculative。到後面看複合圖時，不用重新解讀圖例。

來源：[nvidia/Qwen3.5-122B-A10B-NVFP4 模型卡](https://huggingface.co/nvidia/Qwen3.5-122B-A10B-NVFP4)、[Qwen3.8-27B-FP8 模型卡](https://huggingface.co/Qwen/Qwen3.8-27B-FP8)、[incoai/Qwen3.8-27B-DFlash2 模型卡](https://huggingface.co/incoai/Qwen3.8-27B-DFlash2)

### 3. 報告路線
*從一個 token 的產生，一路推到「能服務幾個人」*

點目錄任一列可以直接跳章。全程約 90 分鐘；壓縮到 50 分鐘的話，
建議保留 01（快帶）、04、06、07、09，把 02、03、05 濃縮成各一頁摘要。

這份報告的主線是「同一個 request」：從 tokenizer 一路到負載測試報告，
每一章問的都是同一件事的不同層次：這一輪 forward 的成本被多少 token 分攤了。

## CH1 一個 token 是怎麼生出來的

### 4. 一個 token 是怎麼生出來的

章節頁停 15 秒就好，把最後一行的 N* 唸出來讓聽眾記住。

### 5. 模型真正看到的是一串 id
*用官方 tokenizer 實際跑一次，id 都是真的*

這一頁的 id 是用 research/tokenize_demo.py 拿官方
tokenizer.json 的 vocab 與 merges 實作 byte-level BPE 跑出來的，不是編的，可以自己重跑。

要把「字數」與「token 數」分開。業務端談字數，容量計算用 token 數。
中文大約 1.6 字一個 token，英文大約 5.5 個字元一個 token；再加上 chat template
與 thinking 標記，短問句的實際 prefill 長度往往是原文的兩倍。

「修正」被切開這件事值得講：它說明 token 不是詞。有些看起來很短的 prompt，
因為用字冷僻，token 數會比預期多很多。

來源：[Qwen3.8-27B tokenizer.json（本報告用它實跑 BPE）](https://huggingface.co/Qwen/Qwen3.8-27B/blob/main/tokenizer.json)、[Qwen3.8-27B config.json](https://huggingface.co/Qwen/Qwen3.8-27B/blob/main/config.json)

### 6. Vocabulary 與 lm_head
*為什麼那張 248,320 × hidden 的表，每一步都要整個讀完*

這一頁專門回答三個常被問的問題。

vocab 是什麼？一張跨語言共用的對照表，但表裡的不是「詞」，
而是 byte-level BPE 切出來的 subword。中文常見詞多半 1 個 token，英文常見字多半 1 個 token，罕見字才被拆開。
Qwen3.8 的 BPE vocab 有 248,044 條，加 33 個特殊 token，共 248,077；
config 宣告 248,320，中間那 243 個是 padding，把長度湊成 128 的倍數讓 kernel 好對齊。

BF16 佔用怎麼算？參數量 = V × d。122B：248,320 × 3,072 =
762,839,040 個參數，每個 BF16 佔 2 bytes，
所以 1,525,678,080 bytes = 1.53 GB。
embedding 與 lm_head 各一份（tie_word_embeddings: false），所以要乘 2。

為什麼 lm_head 每步都要讀完？因為 softmax 是對整個詞彙表做正規化。
你不能只算「可能的那幾個字」的分數就抽樣，分母需要全部 248,320 個 logits。
embedding 只是查表（O(1) 讀一列），lm_head 是矩陣乘法（讀滿 V 列）。
這個不對稱就是 CH7 裡 MTP 起草器很貴的原因：每猜一個 token 就要再讀一次這 1.53 GB。

來源：[Qwen3.8-27B tokenizer.json（本報告用它實跑 BPE）](https://huggingface.co/Qwen/Qwen3.8-27B/blob/main/tokenizer.json)、[Qwen3.5-122B-A10B config.json](https://huggingface.co/Qwen/Qwen3.5-122B-A10B/blob/main/config.json)、[Qwen3.8-27B config.json](https://huggingface.co/Qwen/Qwen3.8-27B/blob/main/config.json)

### 7. Autoregressive：生 T 個 token 就要 T 次 forward
*decode 之所以慢，原因就在這裡*

講到第 5 步時停一下，把「1 個 token vs 整份權重」這個不對稱寫在白板上。
122B NVFP4 的權重是 83.5 GB，B200 頻寬 7.7 TB/s，光讀完就要 10.8 ms。只服務一個人的話，一秒最多 92 個 token，而 GPU 的算力幾乎全閒著。

（實際上 MoE 在 batch 1 只會讀到被路由到的 8/256 個 expert，所以是 12.8 GB、1.66 ms，
一秒 601 個 token 的解析上限。這個修正在 CH3 會補上。）

來源：[Qwen3 Technical Report (arXiv 2505.09388)](https://arxiv.org/abs/2505.09388)、[Inside vLLM: Anatomy of a High-Throughput LLM Inference System](https://blog.vllm.ai/2025/09/05/anatomy-of-vllm.html)

### 8. Decoder layer 內部：兩個 mixer
*token mixer 讓位置之間交換資訊，channel mixer 只在單一位置上做事*

這頁的重點是建立「可替換的插槽」這個心智模型：後面 CH4 把 token mixer 換成
Gated DeltaNet、CH3 把 channel mixer 換成 MoE，架構就從 Qwen3-32B 變成 Qwen3.5-122B-A10B。
只有 token mixer 需要記憶體保存歷史，cache 的形狀就是這樣決定的。

右側的張量形狀值得逐行唸：hidden 3072、Q 是 32×256、K/V 只有 2×256。
Q 的總寬度 8192 比 hidden 還大，這是 Qwen3.5 的特色（head_dim 256 相當大）。

來源：[Qwen3.5-122B-A10B config.json](https://huggingface.co/Qwen/Qwen3.5-122B-A10B/blob/main/config.json)、[Qwen3 Technical Report (arXiv 2505.09388)](https://arxiv.org/abs/2505.09388)

### 9. 兩種 mixer 各自解決一個問題
*「去哪裡拿資訊」與「拿到之後算出什麼」*

這個框架是後面所有架構討論的骨架。CH4 的 Gated DeltaNet 是在換 token mixer，
CH3 的 MoE 是在換 channel mixer，兩者的成本結構完全不同。

為什麼 channel mixer 有存在的必要？因為 attention 只會「搬」，不會「算」。
如果一層裡只有 attention，你很會從各處收集資訊，但不擅長把收集到的東西
轉成更抽象的表示。FFN 的每個 neuron 都是一個 feature detector：
它對輸入的所有維度做加權組合，再過非線性，用來偵測「某種特定的 feature 組合有沒有出現」。

另一個實務推論：channel mixer 不需要任何歷史 cache，所以 MoE 再大也不會增加
每條序列的記憶體，只會增加權重。這解釋了 CH3 裡「MoE 省算力不省記憶體」那句話。

來源：[Qwen3 Technical Report (arXiv 2505.09388)](https://arxiv.org/abs/2505.09388)、[Gated Delta Networks (arXiv 2412.06464)](https://arxiv.org/abs/2412.06464)

### 10. Causal mask：為什麼舊 token 不用重算
*KV cache 之所以成立的前提*

這一頁是 KV cache 的理論基礎，第一版報告直接跳過了。

核心那句話值得慢慢唸：因為是 causal decoder，未來 token 的出現不會改變過去 token
已經算好的 representation 與 K/V。序列每次 +1，但不是整條序列都要重算。

如果沒有這個性質（例如 encoder 的雙向 attention），KV cache 根本不可能存在，
每生一個 token 就得把整條序列重跑一次，成本是 O(T²)。

最後一步是常見的混淆點：很多人以為有了 KV cache，decode 的 attention 就變成 O(1)。
不是。省掉的是「重算歷史 K/V」，沒省掉的是「拿現在的 Q 去查全部歷史 K/V」，那還是 O(T)。
這也是為什麼長 context 的 decode 會越跑越慢。

來源：[Qwen3 Technical Report (arXiv 2505.09388)](https://arxiv.org/abs/2505.09388)、[vLLM PagedAttention 發表文](https://blog.vllm.ai/2023/06/20/vllm.html)

### 11. 為什麼是 KV cache，不是 QKV cache
*Q 是一次性的問題，K/V 才是留給未來查的東西*

一組好用的對照：

Q = 現在這個 token 想查什麼 → 用完丟掉

K = 以前的 token 留下的「可被搜尋的索引」 → 留著

V = 索引命中之後要讀出來的內容 → 留著

這也解釋了 GQA 為什麼可行：不同的 Q head 就算共用同一組 K/V，
因為 query 不同，算出來的 attention 分數還是不同。像同一個資料庫，
不同的研究員問不同的問題。省的是「資料庫」，不是「問問題的能力」。

最後那個對照（append vs 覆寫）是 CH4 的預告：這一個字的差別，
就是 KV cache 隨 context 長大、而 SSM state 不會的全部原因。

來源：[vLLM PagedAttention 發表文](https://blog.vllm.ai/2023/06/20/vllm.html)、[Qwen3 Technical Report (arXiv 2505.09388)](https://arxiv.org/abs/2505.09388)

### 12. KV cache：把算過的東西存起來
*代價是記憶體隨 context 線性成長*

這一頁要讓聽眾意識到 KV cache 是「拿記憶體換計算」的交易，而不是免費的最佳化。
在 262,144 的 context 之下，Qwen3-32B 這種純 Transformer 的 KV cache 會到 64 GiB／條，
單卡放不下兩條。混合架構會出現，就是為了這件事。

來源：[vLLM PagedAttention 發表文](https://blog.vllm.ai/2023/06/20/vllm.html)、[Qwen3 Technical Report (arXiv 2505.09388)](https://arxiv.org/abs/2505.09388)

### 13. GQA：Q 很多、K/V 很少
*KV cache 的大小只跟 KV head 數有關，跟 Q head 數無關*

這裡要點出一個容易被忽略的事：head_dim 256 其實很大（Qwen3-32B 只有 128），
但因為 KV head 只有 2 組，最後 KV/token 還是很小。看 KV cache 只看單一參數會誤判，
必須用完整公式。

Qwen3-32B 的 256 KiB/token 與 122B 的 24 KiB/token 差 10.7 倍，同樣的 HBM 能放的 context 也就差 10 倍。
這是這一版最重要的對照之一。

來源：[Qwen3.5-122B-A10B config.json](https://huggingface.co/Qwen/Qwen3.5-122B-A10B/blob/main/config.json)、[Qwen3.8-27B config.json](https://huggingface.co/Qwen/Qwen3.8-27B/blob/main/config.json)、[Qwen3-32B config.json](https://huggingface.co/Qwen/Qwen3-32B/blob/main/config.json)

### 14. Prefill 與 decode：兩種完全不同的工作
*同樣的權重、同樣的層，成本結構卻相反*

這一頁的概念在全報告裡最重要。之後每一個技術（continuous batching、chunked prefill、
MoE、speculative decoding）都可以用「它把哪一側的浪費補起來」來解釋。

補一個實務數字：prefill 一個 2,048 token 的 prompt 與 decode 一個 token，
讀的權重量一模一樣，但前者做了 2,048 倍的運算。把 prefill 與 decode 混在同一個 batch
（chunked prefill）能同時改善兩邊，道理就在這。

來源：[Inside vLLM: Anatomy of a High-Throughput LLM Inference System](https://blog.vllm.ai/2025/09/05/anatomy-of-vllm.html)、[Qwen3 Technical Report (arXiv 2505.09388)](https://arxiv.org/abs/2505.09388)

### 15. 屋頂線：B200 上的轉折點是 292 token / step
*而且它不會因為量化而移動*

這一頁是第二版新增的，也是整份報告的樞紐。把 N* 的公式推一次：

時間頻寬 = P·b / BW，時間算力 = N·2P / F，兩者相等得 N* = F·b/(2·BW)。

代入 B200：4.5e15 × 1 / (2 × 7.7e12) = 292.2。

精度不變性這個結果很漂亮，聽眾通常會意外。它也推翻了一個常見誤會：
「上了 FP4 就不再是 memory-bound」。FP4 讓你讀得更快也算得更快，
但你在 roofline 上的相對位置沒有動。

要注意這是 dense 峰值、且不含 attention kernel 與 MoE routing 的成本，
所以是解析上限。MoE 的專家讀取量會隨 batch 成長，讓 122B 的實際轉折點推遲到約 1,765 token/step，
CH3 與 CH7 會處理這個修正。

來源：[Lenovo ThinkSystem HGX B200 180GB 產品指南（規格表）](https://lenovopress.lenovo.com/lp2226.pdf)、[NVIDIA Blackwell / HGX B200 datasheet](https://www.nvidia.com/en-us/data-center/hgx/)

## CH2 vLLM 把它變成一個服務

### 16. vLLM 把它變成一個服務

這一章要讓「調參數」變得有跡可循：知道每個旗標作用在哪一層。

### 17. vLLM 的執行堆疊
*六層責任邊界，換模型時只有最下面兩層需要改*

對照官方文件的說法：引擎只要有 request 就反覆呼叫 step()，
每個 step 分成三段：schedule、forward pass、postprocess。

調參對照表：--max-num-seqs 與 --max-num-batched-tokens 作用在第二層；
--gpu-memory-utilization、--kv-cache-dtype、--enable-prefix-caching
作用在第三層；--tensor-parallel-size 在第四層；--enforce-eager 在第五層；
--quantization 在第六層。

來源：[vLLM Architecture Overview](https://docs.vllm.ai/en/latest/design/arch_overview/)、[Inside vLLM: Anatomy of a High-Throughput LLM Inference System](https://blog.vllm.ai/2025/09/05/anatomy-of-vllm.html)

### 18. Continuous batching：空出來的位置立刻補上
*同一次權重讀取被更多 token 分攤*

這是把 CH1 的結論落地的第一個機制。要強調 continuous batching 的重點不在「把 batch 開大」，
而在「讓 batch 的成員每一輪都可以換」。

實務上它帶來一個副作用：同一個 request 的 ITL 會抖動，因為每一輪的同伴數量不同。
CH8 討論百分位時會回來處理這件事。

來源：[Inside vLLM: Anatomy of a High-Throughput LLM Inference System](https://blog.vllm.ai/2025/09/05/anatomy-of-vllm.html)、[vLLM PagedAttention 發表文](https://blog.vllm.ai/2023/06/20/vllm.html)

### 19. 原始示意圖對照
*vLLM 官方部落格的 batching 策略圖*

時間緊的話這一頁可以跳過，前一頁的動畫已經講完了。

來源：[Inside vLLM: Anatomy of a High-Throughput LLM Inference System](https://blog.vllm.ai/2025/09/05/anatomy-of-vllm.html)、[vLLM PagedAttention 發表文](https://blog.vllm.ai/2023/06/20/vllm.html)

### 20. PagedAttention：把 KV cache 當成作業系統的分頁
*邏輯連續、實體不連續，碎片幾乎消失*

在 PagedAttention 之前，系統必須為每個 request 預留「最壞情況」的連續空間，
實際利用率常常只有 20–40%。分頁之後浪費只剩下最後一塊的尾巴。

對混合模型有一個重要的後果：GDN 的 state 大小固定，不適合用 token 為單位分頁。
vLLM 的做法是把 attention 層的 block_size 撐大到能塞得下一份 mamba state，
再把兩者當成同一種「頁」來管理。CH4 會展開。

來源：[vLLM PagedAttention 發表文](https://blog.vllm.ai/2023/06/20/vllm.html)、[Inside vLLM: Anatomy of a High-Throughput LLM Inference System](https://blog.vllm.ai/2025/09/05/anatomy-of-vllm.html)

### 21. 原始示意圖對照
*vLLM 官方部落格的 PagedAttention 動畫*

GIF 保留原始播放速度。若在會議室投影，可以先停在這頁讓聽眾看兩輪。

來源：[vLLM PagedAttention 發表文](https://blog.vllm.ai/2023/06/20/vllm.html)

### 22. Scheduler 一輪做的四個決定
*token budget 怎麼被切開，決定了 TTFT 與 TPOT 的取捨*

這一頁把 CH8/CH9 要量的東西先接起來：TTFT 與 TPOT 不是兩個獨立指標，
它們在排程器裡共用同一個 token budget，是一個蹺蹺板。

觀察指標：如果 num_preempted 持續 > 0，代表 cache 不夠，
應該降 max_num_seqs 或縮 max_model_len，而不是繼續加壓。

來源：[Inside vLLM: Anatomy of a High-Throughput LLM Inference System](https://blog.vllm.ai/2025/09/05/anatomy-of-vllm.html)、[vLLM Architecture Overview](https://docs.vllm.ai/en/latest/design/arch_overview/)

### 23. Chunked prefill 與 prefix caching
*兩個最容易被低估的旗標*

這兩個功能在新版 vLLM 預設都開著，但值得知道它們的代價。特別是 prefix caching：
它會讓 benchmark 數字大幅變好，如果測試資料的前綴重複度和線上不一樣，容量估計就會失真。
CH9 的測試設定表會要求記錄命中率。

來源：[Inside vLLM: Anatomy of a High-Throughput LLM Inference System](https://blog.vllm.ai/2025/09/05/anatomy-of-vllm.html)、[vLLM Hybrid KV Cache Manager 設計文件](https://docs.vllm.ai/en/latest/design/hybrid_kv_cache_manager/)

### 24. 換模型的時候，哪一層真的會動？
*分清「同架構換權重」與「換架構」的成本差距*

決策會議上這一頁通常最實用。它把「支援一個新模型」拆成七個具體項目，
工作量與風險比較好估。

特別提醒 speculative decoding 那一列：起草器與目標模型是綁定的。
換了目標模型的量化版本，起草器不一定要重訓，但一定要重新量接受率，CH7 會說明為什麼。

來源：[vLLM Architecture Overview](https://docs.vllm.ai/en/latest/design/arch_overview/)、[vLLM Qwen GDN linear attention 實作](https://github.com/vllm-project/vllm/blob/main/vllm/model_executor/layers/mamba/gdn/qwen_gdn_linear_attn.py)

## CH3 三個模型的架構帳本

### 25. 三個模型的架構帳本

這一章的重點在可驗證。所有參數都由 config 推導，再跟 HuggingFace 上的 safetensors dtype 統計核對，誤差 < 0.02%。

### 26. 三個模型並排
*換掉 token mixer 與 channel mixer 兩個插槽*

Qwen3.8-27B 與 Qwen3.5-122B-A10B 的 layer_types 陣列在 config 裡是
「三個 linear_attention 接一個 full_attention」重複到底，full_attention_interval = 4。
27B 是 64 層 → 48 GDN + 16 full；122B 是 48 層 → 36 GDN + 12 full。

Qwen3-32B 則是 64 層全部 full attention，head_dim 128、KV head 8 組。

來源：[Qwen3-32B config.json](https://huggingface.co/Qwen/Qwen3-32B/blob/main/config.json)、[Qwen3.8-27B config.json](https://huggingface.co/Qwen/Qwen3.8-27B/blob/main/config.json)、[Qwen3.5-122B-A10B config.json](https://huggingface.co/Qwen/Qwen3.5-122B-A10B/blob/main/config.json)

### 27. Qwen3-32B：純 Transformer 基準線
*所有比較的原點*

把 Qwen3-32B 留在報告裡是刻意的：它提供一個「如果不用混合架構會怎樣」的對照。
所有跟長 context 有關的結論，都可以用它來凸顯差距。

注意它沒有 MTP head（checkpoint 裡沒有 mtp.* 張量），所以要做 speculative decoding
必須另外訓練起草器。這也是評估新模型時要問的問題之一。

來源：[Qwen3-32B config.json](https://huggingface.co/Qwen/Qwen3-32B/blob/main/config.json)、[Qwen3 Technical Report (arXiv 2505.09388)](https://arxiv.org/abs/2505.09388)

### 28. Qwen3.8-27B：混合 token mixer + dense FFN
*本次要上線的 FP8 版本*

27B 這個模型的 head_dim 是 256（很大），但只有 4 組 KV head、16 層 full attention，
所以 KV/token 只有 64 KiB。與 Qwen3-32B 的 256 KiB 相比是 1/4。

要提醒一件容易踩到的事：FP8 checkpoint 指的是權重是 FP8，
KV cache 的精度是另一個獨立的服務端旗標 --kv-cache-dtype，預設是 auto（BF16）。
本報告 27B 的 KV 預設用 BF16 計算，並在試算器提供 FP8 選項。

來源：[Qwen3.8-27B config.json](https://huggingface.co/Qwen/Qwen3.8-27B/blob/main/config.json)、[Qwen3.8-27B 模型卡](https://huggingface.co/Qwen/Qwen3.8-27B)、[Qwen3.8-27B-FP8 模型卡](https://huggingface.co/Qwen/Qwen3.8-27B-FP8)

### 29. Qwen3.5-122B-A10B：混合 token mixer + MoE
*本次要上線的 NVFP4 版本*

這一頁要強調三件事：

① 122B 是總參數，A10B 是每 token 啟用量，兩者用在不同的計算裡（記憶體用前者、算力用後者）。

② NVFP4 讓它變成單卡可跑，這是這次評估最重要的工程結論。

③ MTP head 有 2.52 B 參數而且沒被量化，會吃掉 5 GB。第一版報告完全沒提到這件事。

來源：[Qwen3.5-122B-A10B config.json](https://huggingface.co/Qwen/Qwen3.5-122B-A10B/blob/main/config.json)、[Qwen3.5-122B-A10B 模型卡](https://huggingface.co/Qwen/Qwen3.5-122B-A10B)、[nvidia/Qwen3.5-122B-A10B-NVFP4 模型卡](https://huggingface.co/nvidia/Qwen3.5-122B-A10B-NVFP4)

### 30. MoE：算力與記憶體的錯位
*少算一些 expert，但整組權重都得放在卡上*

最後一步是這一版新增的關鍵洞見，也是 CH7 的伏筆。用機率算一下：
每個 token 選 8/256，N 個 token 之後被碰到的 expert 期望值是
256 × (1 − (1 − 8/256)^N)。N=1 是 8 個、N=32 是 164 個、N=128 是 251 個、N≈290 就幾乎全部。

這代表 MoE 在 batch 1 極省頻寬（只讀 12.8 GB），但在高併發時要讀滿 76 GB，
step 時間下限變成 9.9 ms。這對 speculative decoding 的效益評估影響很大。

注意：這個模型假設 routing 均勻。真實 routing 有偏好（熱門 expert），
實際碰到的 expert 數會比公式略少，所以這是上界。

來源：[Qwen3.5-122B-A10B 模型卡](https://huggingface.co/Qwen/Qwen3.5-122B-A10B)、[Qwen3.5-122B-A10B config.json](https://huggingface.co/Qwen/Qwen3.5-122B-A10B/blob/main/config.json)、[NVFP4 safetensors index](https://huggingface.co/nvidia/Qwen3.5-122B-A10B-NVFP4/blob/main/model.safetensors.index.json)

### 31. 參數帳本：我們怎麼確定沒算錯
*解析公式 vs checkpoint 實際位元組數*

這一頁是報告的可信度基礎。實際做法寫在附錄 B。三個模型的誤差分別是
0.009%、0.018%、0.000%（Qwen3-32B 完全一致，因為它結構最單純）。

0.02% 的殘差來自 MTP head 內部幾個 norm 與 fc 的細節，不影響任何結論。

來源：[NVFP4 safetensors index](https://huggingface.co/nvidia/Qwen3.5-122B-A10B-NVFP4/blob/main/model.safetensors.index.json)、[Qwen3.8-27B-FP8 safetensors index](https://huggingface.co/Qwen/Qwen3.8-27B-FP8/blob/main/model.safetensors.index.json)、[Qwen3-32B config.json](https://huggingface.co/Qwen/Qwen3-32B/blob/main/config.json)

## CH4 Gated DeltaNet：用固定狀態換掉 KV cache

### 32. Gated DeltaNet：用固定狀態換掉 KV cache

全報告技術密度最高的就是這一章。聽眾偏工程的話可以多花點時間。

### 33. Linear attention 的核心想法
*把「查表」換成「維護一個摘要」*

可以用一個類比：full attention 像逐字稿，linear attention 像會議摘要。
逐字稿什麼都查得到但越來越厚；摘要永遠一頁，但細節會流失。
混合架構等於「每四頁摘要就附一頁逐字稿」。

來源：[Gated Delta Networks (arXiv 2412.06464)](https://arxiv.org/abs/2412.06464)、[vLLM Qwen GDN linear attention 實作](https://github.com/vllm-project/vllm/blob/main/vllm/model_executor/layers/mamba/gdn/qwen_gdn_linear_attn.py)

### 34. GDN 的一次 state update
*decay gate 負責遺忘，delta rule 負責改寫*

原論文的式子：Sₜ = Sₜ₋₁(αₜ(I − βₜkₜkₜᵀ)) + βₜvₜkₜᵀ，
其中 αₜ ∈ (0,1) 是資料相關的 decay gate、βₜ ∈ (0,1) 決定 delta rule 的替換強度。

和 Mamba2 的差別就在括號那一項：Mamba2 只有整體衰減 αSₜ₋₁，
DeltaNet 只有定點替換 (I − βkkᵀ)，Gated DeltaNet 兩個都有，
所以既能「快速清空」（α→0）也能「精準改寫」（β→1）。

實作上 α 由 in_proj_a、β 由 in_proj_b 產生，
每個 value head 一個純量；A_log 與 dt_bias 是可學的偏置。

來源：[Gated Delta Networks (arXiv 2412.06464)](https://arxiv.org/abs/2412.06464)、[vLLM Qwen GDN linear attention 實作](https://github.com/vllm-project/vllm/blob/main/vllm/model_executor/layers/mamba/gdn/qwen_gdn_linear_attn.py)

### 35. Delta rule 的直覺
*為什麼不能只是「一直往上加」*

這一頁補的是「為什麼要有 delta rule」的動機，第一版只給了式子沒給直覺。

要澄清一個常見誤解：state 裡面沒有一份「key1、key2、key3」的清單。
它只有一個矩陣。所謂「更新和 apple 有關的方向」，數學上是一次 outer product
（v 是 128×1、k 是 1×128，乘出來就是 128×128），很多甚至全部的矩陣元素都會被改到。
但這是一次矩陣運算，不是迴圈掃過舊資料。

所以複雜度是 O(d_k·d_v)，與 T 無關。這跟 attention 的 O(T·d) 是完全不同的 scaling。

來源：[Gated Delta Networks (arXiv 2412.06464)](https://arxiv.org/abs/2412.06464)、[NVlabs/GatedDeltaNet 官方實作](https://github.com/NVlabs/GatedDeltaNet)

### 36. 三種記憶：Attention、GDN、SSM
*同一個問題的三種答案：過去的資訊要怎麼存*

GDN 與 SSM 常被混為一談，它們確實都是「固定大小的 recurrent state」，
但 state 的數學含義不同：

GDN 的 state 比較像 associative memory，是一個 key→value 的壓縮字典，
用 q 去查（o = S·q）。

SSM（Mamba 那一系）的 state 比較像動態系統的內部狀態，
h_t = A·h_{t−1} + B·x_t，不是字典。Mamba 的貢獻是讓 A、B 隨輸入動態改變，
所以模型可以學「這個 token 重要，留著；那個不重要，忘掉」。

另外一個有用的說法：模型權重是 slow weights，整個推論過程都不變；
GDN 的 state 是 fast weights，每個 token 都在改。o = S·q 看起來就像一個 linear layer，
只是那個「權重」在 decode 過程中一直被改寫。

還有一個容易被忽略的實務點：GDN 的 O(1) 是對 context 長度而言，
不代表計算量小。每個 decode token 都要把整份 state 從 HBM 讀出來、改完再寫回去。
低 batch 的 GDN decode 一樣可能是 memory-bandwidth-bound。演算法複雜度不等於 GPU 瓶頸。

來源：[Gated Delta Networks (arXiv 2412.06464)](https://arxiv.org/abs/2412.06464)、[Mamba: Linear-Time Sequence Modeling with Selective State Spaces](https://arxiv.org/abs/2312.00752)、[HuggingFace transformers：Qwen3.5 模型文件](https://github.com/huggingface/transformers/blob/main/docs/source/en/model_doc/qwen3_5.md)

### 37. 與 Mamba2 / DeltaNet 的關係
*同一條家族樹上的三個節點*

這一頁的實務價值：知道 GDN 走 mamba 路徑之後，就知道要去哪裡找設定與問題。
例如 --mamba-ssm-cache-dtype 可以把 recurrent state 從 FP32 降到 BF16，
直接把 144 MiB 砍成 72 MiB。CH6 會提到這個旋鈕，但品質要先驗過。

來源：[Gated Delta Networks (arXiv 2412.06464)](https://arxiv.org/abs/2412.06464)、[vLLM mamba_utils.py（state shape 計算）](https://github.com/vllm-project/vllm/blob/main/vllm/model_executor/layers/mamba/mamba_utils.py)

### 38. 一次 decode 走完全部層
*兩種 cache 必須同步前進*

這頁要建立一個直覺：一次 decode 同時做了兩種完全不同性質的記憶體操作。
一種是「就地更新固定區塊」（隨機讀寫、大小不變），一種是「append 到成長中的緩衝區」。
它們的分頁策略、eviction 策略、prefix caching 能力都不一樣。

可以按頁面上的模型切換看 27B（64 層、16 組）。兩者的 recurrent state 竟然都剛好是 144 MiB，
因為 36×64 = 48×48 = 2,304 個 value head。這個巧合很好記。

來源：[Qwen3.5-122B-A10B config.json](https://huggingface.co/Qwen/Qwen3.5-122B-A10B/blob/main/config.json)、[Qwen3.8-27B config.json](https://huggingface.co/Qwen/Qwen3.8-27B/blob/main/config.json)、[vLLM Hybrid KV Cache Manager 設計文件](https://docs.vllm.ai/en/latest/design/hybrid_kv_cache_manager/)

### 39. Chunkwise prefill：GDN 怎麼吃長 prompt
*chunk 內平行、chunk 之間只傳一個狀態*

上面那句引文是 GDN 能跑得快的關鍵：gate 只做逐元素乘法，
不破壞矩陣乘法的結構，所以還是能用 Tensor Core。
這也是為什麼 GDN 的 prefill 吞吐可以和 attention 同級。

對 serving 的意義：混合模型的 prefill 不會因為 GDN 而變慢；
差異在 decode（GDN 省很多）與 prefix caching（GDN 目前吃虧）。

來源：[Gated Delta Networks (arXiv 2412.06464)](https://arxiv.org/abs/2412.06464)、[vLLM Qwen GDN linear attention 實作](https://github.com/vllm-project/vllm/blob/main/vllm/model_executor/layers/mamba/gdn/qwen_gdn_linear_attn.py)、[vLLM Hybrid KV Cache Manager 設計文件](https://docs.vllm.ai/en/latest/design/hybrid_kv_cache_manager/)

### 40. vLLM 怎麼同時管兩種 cache
*Hybrid KV Cache Manager 的三個設計決定*

這一頁是第二版新增的，實務上也最容易踩雷。
第一版只說「vLLM 會管」，沒有講清楚 page size 對齊與 prefix caching 交集的代價。

檢查方式：啟動時 vLLM 會在 log 印出 KV cache group 的配置與 block size。
如果看到很大的 block_size（例如 > 256），就要知道小 request 的碎片會偏高，
公式算出來的併發數要打個折扣。

來源：[vLLM Hybrid KV Cache Manager 設計文件](https://docs.vllm.ai/en/latest/design/hybrid_kv_cache_manager/)、[vLLM mamba_utils.py（state shape 計算）](https://github.com/vllm-project/vllm/blob/main/vllm/model_executor/layers/mamba/mamba_utils.py)

## CH5 量化：NVFP4 與 FP8

### 41. 量化：NVFP4 與 FP8

這一章是第二版新增的。第一版假設 BF16，記憶體數字全部偏大 2–3 倍。

### 42. NVFP4 與 FP8 的位元佈局
*兩層 scale 是 NVFP4 能在 4 bit 保住精度的關鍵*

三個容易搞混的點：

① NVFP4 與 OCP 的 MXFP4 不同：block 是 16 不是 32，scale 是 FP8-E4M3 不是 2 的冪次（E8M0）。
所以 NVFP4 精度較好，但 scale 的儲存開銷是 MXFP4 的兩倍。

② 「4-bit 模型」實際上是 4.5 bit，算記憶體時要用 4.5。

③ FP8 的 blockwise-128 是二維的 128×128 區塊，checkpoint 裡叫 weight_scale_inv。

Blackwell 的張量核心原生支援這個格式，所以不需要在 kernel 裡反量化再算，比軟體量化快的原因就在這。

來源：[NVFP4 格式說明（Modal LLM Almanac）](https://modal.com/llm-almanac/block-quants)、[NVFP4：Blackwell microscaling 4-bit float](https://zeroentropy.dev/concepts/nvfp4/)、[NVFP4 hf_quant_config.json](https://huggingface.co/nvidia/Qwen3.5-122B-A10B-NVFP4/blob/main/hf_quant_config.json)、[Qwen3.8-27B-FP8 模型卡](https://huggingface.co/Qwen/Qwen3.8-27B-FP8)

### 43. checkpoint 實際量化了哪些張量
*直接看 safetensors 的 weight map，不用猜*

這一頁的發現大概是整份報告裡最有價值的，而且可以自己驗。

對 122B 的實務意涵：因為 36 層 GDN 全部維持 BF16，它們在 batch 1 的 decode 佔了
6.37 GB 的記憶體流量，是總量 12.8 GB 的一半。也就是說 NVFP4 只解決了 expert 那一半，
另一半仍然是 BF16 的頻寬成本。想再快，得等 GDN 的 FP8 / FP4 kernel。

對 27B：GDN 的 in_proj_a、in_proj_b、conv1d、
A_log、dt_bias 保持 BF16。這些是決定 decay gate 的參數，
數值敏感度高，量化下去會直接壞掉。

來源：[NVFP4 safetensors index](https://huggingface.co/nvidia/Qwen3.5-122B-A10B-NVFP4/blob/main/model.safetensors.index.json)、[Qwen3.8-27B-FP8 safetensors index](https://huggingface.co/Qwen/Qwen3.8-27B-FP8/blob/main/model.safetensors.index.json)、[NVFP4 hf_quant_config.json](https://huggingface.co/nvidia/Qwen3.5-122B-A10B-NVFP4/blob/main/hf_quant_config.json)、[nvidia/Qwen3.5-122B-A10B-NVFP4 模型卡](https://huggingface.co/nvidia/Qwen3.5-122B-A10B-NVFP4)

### 44. 省了多少
*權重、KV cache 與可用 cache 空間的三筆帳*

三張表的重點：

① 權重省下的空間全部變成 cache 空間，而 cache 空間直接等於併發數。

② KV cache 量化是另一個獨立旋鈕，效果同樣大。

③ MTP head 沒被量化這件事在官方文件裡沒有明說，是從 weight map 推出來的。

來源：[nvidia/Qwen3.5-122B-A10B-NVFP4 模型卡](https://huggingface.co/nvidia/Qwen3.5-122B-A10B-NVFP4)、[Qwen3.8-27B-FP8 模型卡](https://huggingface.co/Qwen/Qwen3.8-27B-FP8)、[NVFP4 safetensors index](https://huggingface.co/nvidia/Qwen3.5-122B-A10B-NVFP4/blob/main/model.safetensors.index.json)、[Qwen3.8-27B-FP8 safetensors index](https://huggingface.co/Qwen/Qwen3.8-27B-FP8/blob/main/model.safetensors.index.json)

### 45. 精度代價
*NVIDIA 官方的評測：NVFP4 對 FP8 的差距在 0.6 分以內*

態度很簡單：量化的精度風險是可量測的，別用感覺討論。
建議的驗收流程：拿一組自家的 200–500 題代表性任務，BF16 / FP8 / NVFP4 各跑一次，
看分數差與輸出格式合規率。

引文那句話也解釋了為什麼只有 routed expert 被量化：官方寫得很清楚，
量化範圍是 MoE 內部 transformer block 的線性算子。

來源：[nvidia/Qwen3.5-122B-A10B-NVFP4 模型卡](https://huggingface.co/nvidia/Qwen3.5-122B-A10B-NVFP4)

### 46. 量化不會移動屋頂線的轉折點
*它讓兩邊同時變快，你的相對位置沒動*

這一頁把 CH1 的核心洞見與本章接起來。常見的錯誤預期是
「上了 FP4 之後 GPU 就會被算力綁住」，實際上不會。

唯一會移動 N* 的，是硬體本身的 FLOP/byte 比。B200 是 584 FLOP/byte
（FP8 dense），所以 N* = 292。換到別的卡就要重算。

來源：[Lenovo ThinkSystem HGX B200 180GB 產品指南（規格表）](https://lenovopress.lenovo.com/lp2226.pdf)、[NVFP4 格式說明（Modal LLM Almanac）](https://modal.com/llm-almanac/block-quants)

## CH6 單卡記憶體帳本

### 47. 單卡記憶體帳本

這一章就是把 SSM cache 跟 KV cache 分開算。

### 48. 兩種 cache、兩種成長曲線
*一條斜線，一條水平線*

這張圖是這一章的地圖。三個觀察：

① 純 Transformer 沒有起跳成本，但斜率很陡。

② 混合模型有一個約 150 MiB 的固定起跳成本，之後斜率平緩很多。

③ 所以短對話的場景，混合模型的併發上限反而由 SSM state 決定，
不在 KV cache。這一點跟直覺相反，第一版報告也漏掉了。

來源：[Qwen3.5-122B-A10B config.json](https://huggingface.co/Qwen/Qwen3.5-122B-A10B/blob/main/config.json)、[Qwen3.8-27B config.json](https://huggingface.co/Qwen/Qwen3.8-27B/blob/main/config.json)、[vLLM mamba_utils.py（state shape 計算）](https://github.com/vllm-project/vllm/blob/main/vllm/model_executor/layers/mamba/mamba_utils.py)、[vLLM Hybrid KV Cache Manager 設計文件](https://docs.vllm.ai/en/latest/design/hybrid_kv_cache_manager/)

### 49. KV cache 的容量計算
*先算每條序列，再考慮並行、分片與配置開銷*

公式要逐項唸一次，特別是 2 的來源（K 和 V 各一份）。

那個「TP 最多切到 2」很重要：122B 的 num_key_value_heads = 2，
所以 TP=4 或 TP=8 時 KV head 不夠切，vLLM 會複製 KV head，
每張卡的 KV cache 不會再變小。這也是 NVFP4 單卡部署更划算的理由之一。

來源：[Qwen3.5-122B-A10B config.json](https://huggingface.co/Qwen/Qwen3.5-122B-A10B/blob/main/config.json)、[Qwen3.8-27B config.json](https://huggingface.co/Qwen/Qwen3.8-27B/blob/main/config.json)、[Qwen3-32B config.json](https://huggingface.co/Qwen/Qwen3-32B/blob/main/config.json)、[nvidia/Qwen3.5-122B-A10B-NVFP4 模型卡](https://huggingface.co/nvidia/Qwen3.5-122B-A10B-NVFP4)

### 50. SSM cache 的容量計算
*固定大小的 recurrent 矩陣，加一小段 convolution 歷史*

兩條公式都寫出來了。conv state 那條裡的 num_spec 是 CH7 的伏筆。

要強調 SSM state 是「每條序列一份」，不是「每 token 一份」。
所以它跟 --max-num-seqs 直接相乘，跟 --max-model-len 無關。

來源：[vLLM mamba_utils.py（state shape 計算）](https://github.com/vllm-project/vllm/blob/main/vllm/model_executor/layers/mamba/mamba_utils.py)、[vLLM Qwen GDN linear attention 實作](https://github.com/vllm-project/vllm/blob/main/vllm/model_executor/layers/mamba/gdn/qwen_gdn_linear_attn.py)、[Qwen3.5-122B-A10B config.json](https://huggingface.co/Qwen/Qwen3.5-122B-A10B/blob/main/config.json)、[Qwen3.8-27B config.json](https://huggingface.co/Qwen/Qwen3.8-27B/blob/main/config.json)

### 51. 交會點：context 多長時 KV 才追上 SSM
*短對話的併發上限其實由 SSM state 決定*

這一頁把「該調哪個旗標」直接接到 workload 上，最好用。

可以現場算給聽眾看：122B 在 8k context 下，一條序列 197 MiB，
其中 149 MiB 是 SSM state（76%）。把 max_model_len 從 262k 降到 8k
不會改變這 149 MiB，只會影響 vLLM 預留的上限。

來源：[Qwen3.5-122B-A10B config.json](https://huggingface.co/Qwen/Qwen3.5-122B-A10B/blob/main/config.json)、[Qwen3.8-27B config.json](https://huggingface.co/Qwen/Qwen3.8-27B/blob/main/config.json)、[vLLM mamba_utils.py（state shape 計算）](https://github.com/vllm-project/vllm/blob/main/vllm/model_executor/layers/mamba/mamba_utils.py)

### 52. 互動試算：B200 單卡的完整帳本
*自己調參數看併發上限怎麼變*

示範順序建議：

① 預設（122B、FP8 KV、開 spec、8k、32 併發）→ 看 SSM 那條佔多大。

② 把 context 拉到 262k → KV 立刻爆掉，剩餘變負。

③ 關掉 spec → 省下 5.05 GB。

④ 切到 27B → 權重只剩 28.75 GiB，cache 空間變成兩倍多。

⑤ 把 KV 切到 BF16 → 看 27B 的 KV 直接翻倍。

activation 的估計是教學用途；實際由 vLLM 依 max_num_batched_tokens、
CUDA graph 捕捉的 batch 尺寸與 kernel workspace 決定，開機時 log 會印出實際保留量。

來源：[Lenovo ThinkSystem HGX B200 180GB 產品指南（規格表）](https://lenovopress.lenovo.com/lp2226.pdf)、[nvidia/Qwen3.5-122B-A10B-NVFP4 模型卡](https://huggingface.co/nvidia/Qwen3.5-122B-A10B-NVFP4)、[Qwen3.8-27B-FP8 模型卡](https://huggingface.co/Qwen/Qwen3.8-27B-FP8)、[vLLM mamba_utils.py（state shape 計算）](https://github.com/vllm-project/vllm/blob/main/vllm/model_executor/layers/mamba/mamba_utils.py)

### 53. 併發上限與 context 的取捨
*同一張卡，你可以選「多人短對話」或「少人長文件」*

最後那個警告很重要：很多人把「max_num_seqs 設多少」當成容量，
其實記憶體上限往往是 SLO 上限的好幾倍。把 max_num_seqs 開到記憶體上限，
只會讓延遲爆掉、佇列變長，goodput 反而下降。

正確做法：記憶體上限決定硬性天花板，SLO 測試決定實際設定值。

來源：[Qwen3.5-122B-A10B config.json](https://huggingface.co/Qwen/Qwen3.5-122B-A10B/blob/main/config.json)、[Qwen3.8-27B config.json](https://huggingface.co/Qwen/Qwen3.8-27B/blob/main/config.json)

## CH7 Speculative decoding 對 serving 的影響

### 54. Speculative decoding 對 serving 的影響

這一整章是第二版新增的，大概也是最實用的一章。

### 55. 為什麼可以「猜」
*decode 時算力大量閒置，那塊算力是免費的*

把 CH1 的屋頂線接回來：只要 N < 292，加 token 幾乎不用付錢。
speculative decoding 就是「用同一次權重讀取，多送幾個 token 進去」的另一種辦法，
跟加大 batch 是同一件事的兩種做法。

差別在於：加大 batch 需要有更多使用者；speculative decoding 在只有少數使用者時
也能把 N 撐大。所以它是低併發、低延遲場景的利器。

來源：[vLLM Speculative Decoding 文件](https://docs.vllm.ai/en/latest/features/speculative_decoding/)、[SmartSpec：以 goodput 最佳化 speculative decoding](https://arxiv.org/abs/2406.14066)

### 56. 一輪 draft → verify → accept
*被拒絕之後整串丟掉，但一定會前進至少一格*

三個常見誤解要澄清：

① 「猜錯會讓品質變差」：不會。rejection sampling 在數學上保證輸出分布不變，
vLLM 文件的說法是「theoretically lossless up to the precision limits of hardware numerics」。

② 「最壞情況會比不猜慢」：就 token 數而言不會（一定前進 1 格），
但就時間而言會，因為多付了起草成本與驗證的額外計算。

③ 「被拒絕的位置後面那些草稿還可以留著」：不行，它們是以錯誤前綴為條件產生的。

來源：[vLLM Speculative Decoding 文件](https://docs.vllm.ai/en/latest/features/speculative_decoding/)、[EAGLE-3 (arXiv 2503.01840)](https://arxiv.org/abs/2503.01840)

### 57. MTP 與 DFlash2：兩種完全不同的起草器
*一個是序列式跑 k 次，一個是一次猜完整塊*

DFlash 論文的說法：drafting cost 是 T_draft = t_parallel，
相對於 autoregressive drafting 的 T_draft = γ·t_step。
它用非因果 attention 讓每個 query 同時看到目標模型的 hidden state 與所有 mask token，
一次產生整塊草稿，再用一個輕量 selector（selector_rank 256、top-k 16）挑一條連貫的路徑。

另外 DFlash 把目標模型 5 層的 hidden state 注入到起草器每一層的 KV
（config 裡的 target_layer_ids: [5, 19, 33, 47, 61]），
這讓接受長度能隨草稿深度成長，而不是很快飽和。

來源：[Qwen3.5-122B-A10B 模型卡](https://huggingface.co/Qwen/Qwen3.5-122B-A10B)、[incoai/Qwen3.8-27B-DFlash2 模型卡](https://huggingface.co/incoai/Qwen3.8-27B-DFlash2)、[DFlash: Block Diffusion for Flash Speculative Decoding](https://arxiv.org/abs/2602.06036)、[NVIDIA：Boost Inference up to 15x with DFlash](https://developer.nvidia.com/blog/boost-inference-performance-up-to-15x-on-nvidia-blackwell-using-dflash-speculative-decoding/)、[EAGLE-3 (arXiv 2503.01840)](https://arxiv.org/abs/2503.01840)

### 58. 兩個起草器的規格對照
*本次上線設定的完整參數*

這一頁把使用者提供的設定（122B 用 MTP 猜 3、27B 用 DFlash2 猜 7）
變成可查證的規格表。

要特別提醒 k 的選擇邏輯：MTP 因為起草成本 ∝ k，k 大了不划算，
所以官方建議 2–4；DFlash2 因為起草成本固定，把 k 拉到 7 幾乎沒有額外起草成本，
代價是驗證時要多算 token，正好接到下一頁的屋頂線分析。

來源：[Qwen3.5-122B-A10B 模型卡](https://huggingface.co/Qwen/Qwen3.5-122B-A10B)、[incoai/Qwen3.8-27B-DFlash2 模型卡](https://huggingface.co/incoai/Qwen3.8-27B-DFlash2)、[DFlash: Block Diffusion for Flash Speculative Decoding](https://arxiv.org/abs/2602.06036)、[Qwen3.5-122B-A10B config.json](https://huggingface.co/Qwen/Qwen3.5-122B-A10B/blob/main/config.json)、[Qwen3.8-27B config.json](https://huggingface.co/Qwen/Qwen3.8-27B/blob/main/config.json)

### 59. 接受長度 τ：報酬遞減得很快
*k 加大不會等比例變好*

公式 τ = Σ_{i=0..k} α^i = (1−α^(k+1))/(1−α)，是 SmartSpec 論文與
「Interpretable Latency Model」論文都用的估計式。

要注意這個式子假設每個位置的接受率相同，實際上會逐位置下降（見兩頁後的實測）。
所以它是樂觀估計；實際的 τ 要從引擎的 acceptance metrics 讀。

vLLM 有 per-request acceptance metrics，上線後應該持續監控 τ，
它是判斷「該不該繼續開 spec」最直接的訊號。

來源：[EAGLE-3 (arXiv 2503.01840)](https://arxiv.org/abs/2503.01840)、[incoai/Qwen3.8-27B-DFlash2 模型卡](https://huggingface.co/incoai/Qwen3.8-27B-DFlash2)、[An Interpretable Latency Model for Speculative Decoding](https://arxiv.org/abs/2605.15051)、[SmartSpec：以 goodput 最佳化 speculative decoding](https://arxiv.org/abs/2406.14066)

### 60. (k+1)× 的 token 撞上屋頂線
*這是 speculative decoding 對 serving 最大的影響*

這一頁是全章最重要的一張圖，也最直接回答「speculative decoding 對 serving 的影響」。

推導很簡單：驗證階段一次送 N = B × (k+1) 個 token。CH1 已經證明
N < N* 時加 token 幾乎免費、N > N* 時線性收費。所以：

B_安全 ≈ N* / (k+1)。

27B 配 k=7 → B ≈ 37；122B 配 k=3 → B ≈ 73（但 MoE 的修正讓它實際更寬）。

這也解釋了一個常見現象：同一組設定在壓測初期（低併發）看起來很棒，
壓到中段突然變差，就是越過了這條線。

模型的假設要講清楚：這是解析上限，只算權重讀取與張量運算，
不含 attention kernel、MoE routing、all-to-all、取樣與排程開銷。
真實曲線的形狀相同，但轉折會更早、絕對值更低。

來源：[SmartSpec：以 goodput 最佳化 speculative decoding](https://arxiv.org/abs/2406.14066)、[An Interpretable Latency Model for Speculative Decoding](https://arxiv.org/abs/2605.15051)、[Lenovo ThinkSystem HGX B200 180GB 產品指南（規格表）](https://lenovopress.lenovo.com/lp2226.pdf)

### 61. 互動試算：spec decoding 划不划算
*自己調 k、α 與併發數*

示範腳本：

① 預設（27B + DFlash2、k=7、α=0.80、B=16）→ 看到 4.4× 與 TPOT 大幅改善。

② 把 B 拉到 64 → 加速掉到 2.5×。

③ 拉到 200 → 掉到 1× 以下，TPOT 反而比不開還差。

④ 把 k 降到 3 → 在高併發時反而比 k=7 好（因為 (k+1) 較小）。

⑤ 把 α 降到 0.6（模擬長 context）→ 整條曲線塌下來。

⑥ 切到 122B + MTP → 看形狀完全不同。

結論：k 應該隨負載動態調整。vLLM 已有 Dynamic Speculative Decoding
可以依 QPS 調整，SmartSpec 論文的做法是依 goodput 決定每一輪的 k，
高負載時自動歸零。

來源：[SmartSpec：以 goodput 最佳化 speculative decoding](https://arxiv.org/abs/2406.14066)、[An Interpretable Latency Model for Speculative Decoding](https://arxiv.org/abs/2605.15051)、[incoai/Qwen3.8-27B-DFlash2 模型卡](https://huggingface.co/incoai/Qwen3.8-27B-DFlash2)

### 62. 長 context 會吃掉接受率
*實測案例：從加速 129% 變成減速 51%*

這是全章最該記住的實務警告，而且有公開實測資料撐著。

為什麼會這樣？兩個原因：

① 起草器看到的上下文越長，任務越難、分布越發散，猜中率自然下降。

② context 一長，目標模型自己的每一步也更貴（attention 讀取量隨 T 成長），
但起草器的成本沒有等比例下降，所以相對開銷變高。

DFlash 論文也觀察到同樣現象（base model 的接受長度在 4k 之後開始下降），
他們的解法是針對長 context 微調起草器，能把 16k 的 τ 從 3.61 拉回 6.05。
如果我們的 workload 是長文件，這是一個可以評估的方向。

來源：[vLLM #47602：MTP 接受率隨 context 衰減](https://github.com/vllm-project/vllm/issues/47602)、[DFlash: Block Diffusion for Flash Speculative Decoding](https://arxiv.org/abs/2602.06036)

### 63. 其他三個副作用
*記憶體、prefix caching 與延遲抖動*

這三個副作用都不是致命的，但都會讓「開了 spec 之後 benchmark 數字變差」
變得難以解釋。先知道它們存在，排查時就快很多。

特別是 prefix caching 那個：如果你的 workload 前綴重複度高，
關掉 spec 反而可能更快。這一定要 A/B 測。

來源：[vLLM mamba_utils.py（state shape 計算）](https://github.com/vllm-project/vllm/blob/main/vllm/model_executor/layers/mamba/mamba_utils.py)、[vLLM #38182：MTP 降低 prefix cache 命中率](https://github.com/vllm-project/vllm/issues/38182)、[vLLM Speculative Decoding 文件](https://docs.vllm.ai/en/latest/features/speculative_decoding/)

### 64. 什麼時候該開、什麼時候該關
*一張可以直接用的決策表*

這一頁可以直接印出來當 checklist。

要強調最後一格：本報告給的是基於解析模型的初步判斷，
不是實測結論。決策要等 CH9 的兩階段測試跑完，用同一組 SLO 比較開／關的 goodput。

來源：[SmartSpec：以 goodput 最佳化 speculative decoding](https://arxiv.org/abs/2406.14066)、[An Interpretable Latency Model for Speculative Decoding](https://arxiv.org/abs/2605.15051)、[vLLM Speculative Decoding 文件](https://docs.vllm.ai/en/latest/features/speculative_decoding/)、[vLLM #47602：MTP 接受率隨 context 衰減](https://github.com/vllm-project/vllm/issues/47602)

### 65. 上線設定與命令
*可以直接複製的設定*

兩段指令的來源：122B 是 NVIDIA 模型卡的部署範例（原本用
--tensor-parallel-size 1，我們保留），加上 Qwen 模型卡的 MTP 設定；
27B 是 incoai 模型卡的 vLLM 範例。

--max-model-len 我填 65536 而不是原生的 262144，
因為 CH6 顯示跑滿 262k 只能放個位數的序列。實務上應該依 workload 的
p99 context 長度設定，留一點餘裕即可。

來源：[nvidia/Qwen3.5-122B-A10B-NVFP4 模型卡](https://huggingface.co/nvidia/Qwen3.5-122B-A10B-NVFP4)、[incoai/Qwen3.8-27B-DFlash2 模型卡](https://huggingface.co/incoai/Qwen3.8-27B-DFlash2)、[Qwen3.5-122B-A10B 模型卡](https://huggingface.co/Qwen/Qwen3.5-122B-A10B)、[vLLM Speculative Decoding 文件](https://docs.vllm.ai/en/latest/features/speculative_decoding/)

## CH8 延遲指標的定義與陷阱

### 66. 延遲指標的定義與陷阱

這一章短，但值得要求所有人照同一份定義寫報告。

### 67. 一個 request 的完整時間軸
*四個指標各自量的是哪一段*

ITL 與 TPOT 最容易被混用。舉例：100 個 request，每個 500 個 token。

ITL 的樣本數是 100 × 499 = 49,900；

TPOT 的樣本數是 100。

所以「p95 ITL」與「p95 TPOT」是兩個完全不同的數字，前者會被單一次的長停頓拉高，
後者被平均掉。開了 speculative decoding 之後這個差距會更大。

來源：[vllm bench serve CLI 文件](https://docs.vllm.ai/en/latest/cli/bench/serve/)、[vLLM goodput metric（PR #9338）](https://github.com/vllm-project/vllm/pull/9338)

### 68. 四個指標的精確定義
*以及各自的陷阱*

這一頁可以直接拿去當團隊的指標定義規範。

「開了 spec 之後 ITL 變雙峰」這件事值得強調：如果 SLO 寫的是「p99 ITL < 50 ms」，
開了 spec 之後可能反而不合格，即使使用者感受更好。這時要把 SLO 改寫成 TPOT。

來源：[vllm bench serve CLI 文件](https://docs.vllm.ai/en/latest/cli/bench/serve/)、[vLLM goodput metric（PR #9338）](https://github.com/vllm-project/vllm/pull/9338)、[Inside vLLM: Anatomy of a High-Throughput LLM Inference System](https://blog.vllm.ai/2025/09/05/anatomy-of-vllm.html)

### 69. 高併發時，每個指標各自怎麼動
*同一台機器，128 個人同時上門*

這一頁把四個指標的「變化方向」講清楚，下一頁再用模擬器把數字算出來。

最關鍵的是 ITL 那一列。它不會一路變差；在屋頂線轉折點之前幾乎不動。
27B 的 N* 是 292 token/step，所以在併發 48（240 token/step）之前，
加人幾乎不影響打字速度。這跟很多人的直覺相反。

另一個實務提醒：TTFT 包含排隊，TPOT 不包含。所以系統過載時，
已經進場的人可能感覺一切正常（TPOT 沒變），但門外排隊的人等到天荒地老。
只監控 TPOT 會完全看不到這件事。

來源：[Inside vLLM: Anatomy of a High-Throughput LLM Inference System](https://blog.vllm.ai/2025/09/05/anatomy-of-vllm.html)、[vllm bench serve CLI 文件](https://docs.vllm.ai/en/latest/cli/bench/serve/)

### 70. max_num_seqs 該掐多少
*128 個人上門，48 對 96 的差別*

把數字唸一次（27B FP8、128 人、in 2048 / out 512）：

max_num_seqs = 48：48 個在跑、80 個排隊。每 step 240 個 token，還在
memory-bound 區，所以 ITL 停在 3.50 ms 的地板。但 TTFT 是 3.08 秒。

max_num_seqs = 96：96 個在跑、32 個排隊。每 step 480 個 token，
已經越過 N* = 292，變成 compute-bound，ITL 漲到 5.74 ms。
但 TTFT 只有 1.02 秒，E2E 從 4.87 秒降到 3.96 秒，吞吐從 13,728 漲到 16,718 tok/s。

所以你的直覺是對的：掐小確實會讓 ITL 變短，也確實會因為強制排隊讓 TTFT 變長。
但要注意，這筆交易在固定負載下是虧的：只有 ITL 一項變好，其他三項都變差。

那什麼時候該掐？三個正當理由：

① 記憶體：max_num_seqs × 每序列 cache 不能超過預算，否則會不斷 preempt。這是硬限制。

② ITL 是主要 SLO：串流體驗掛帥、而且你願意用 TTFT 去換。

③ 保護已進場的人：與其讓所有人一起慢慢變差，不如限制併發，
讓進場的人有可預測的品質。這是 admission control 的邏輯。

來源：[Inside vLLM: Anatomy of a High-Throughput LLM Inference System](https://blog.vllm.ai/2025/09/05/anatomy-of-vllm.html)、[vllm bench serve CLI 文件](https://docs.vllm.ai/en/latest/cli/bench/serve/)

### 71. 自己模擬一次高併發
*拉人數與 max_num_seqs，看四個指標往哪邊跑*

建議的操作順序：

① 預設（27B、128 人、max_num_seqs 48）→ 看 TTFT 3 秒、ITL 3.5 ms。

② 把 max_num_seqs 拉到 128 → TTFT 掉到 31 ms，但 ITL 漲到 7.66 ms。

③ 把人數拉到 256 → 不管怎麼調，TTFT 都回不去了。這時候該加卡，不是調參數。

④ 打開 speculative decoding → 看 ITL 大幅下降，但 tok/step 暴增，
很快就撞上屋頂線。

⑤ 把輸出長度改成 128（短回覆）→ TTFT 在 E2E 裡的佔比變高，
這時 max_num_seqs 的影響更明顯。

模型的假設寫在圖下方。它沒有擬合參數，但也不含 attention kernel、
排程與通訊開銷，所以實測會比它慢。用它看趨勢與轉折位置，不要看絕對值。

來源：[Inside vLLM: Anatomy of a High-Throughput LLM Inference System](https://blog.vllm.ai/2025/09/05/anatomy-of-vllm.html)、[vllm bench serve CLI 文件](https://docs.vllm.ai/en/latest/cli/bench/serve/)

### 72. 百分位與 goodput
*平均值會騙人，goodput 才是使用者拿到的服務*

vLLM 的 bench serve 支援 --goodput 參數，
可以一次指定多個 SLO（例如 --goodput ttft:500 tpot:40），
輸出 request goodput。這是最接近「使用者體驗」的單一數字。

強調一個實務場景：把併發從 32 加到 128，throughput 可能從 6,000 漲到 9,000 tok/s，
但 p95 TTFT 從 600 ms 變成 4,000 ms，goodput 反而從 5,800 掉到 0。
只看 throughput 會做出錯誤的決策。

來源：[vLLM goodput metric（PR #9338）](https://github.com/vllm-project/vllm/pull/9338)、[vllm bench serve CLI 文件](https://docs.vllm.ai/en/latest/cli/bench/serve/)

## CH9 兩階段負載測試

### 73. 兩階段負載測試

這一章要交出來的是一份別人可以重跑的測試報告，而不是一個最高 tok/s 的數字。

### 74. 閉環 vs 開環
*兩種測試回答的是完全不同的問題*

負載測試最常見的方法論錯誤就是這個。閉環測試（大多數工具的預設）
會讓系統看起來一直很穩，因為 client 在幫伺服器限流。

vLLM 的 bench serve 用 --request-rate 控制開環到達率，
用 --max-concurrency 控制閉環併發。兩個都要跑。

來源：[vllm bench serve CLI 文件](https://docs.vllm.ai/en/latest/cli/bench/serve/)

### 75. Stage 1：找飽和點
*控制併發，量 achieved throughput*

操作方式：把上面切到「Stage 1 閉環」，拖動控制變數看兩張圖怎麼變。

Stage 1 要記錄的：

① throughput 開始飽和的併發數，對照 CH1 的 N* ≈ 292；
如果沒開 spec，飽和點的併發數應該接近 N*。

② 飽和之後 TPOT 的成長速率。

③ 有沒有出現 preempt（那代表 cache 不足，不是算力飽和）。

再次提醒：曲線是教學示例，用排隊理論生成。

來源：[vllm bench serve CLI 文件](https://docs.vllm.ai/en/latest/cli/bench/serve/)、[Inside vLLM: Anatomy of a High-Throughput LLM Inference System](https://blog.vllm.ai/2025/09/05/anatomy-of-vllm.html)

### 76. Stage 2：找 SLO 容量
*控制到達率，看佇列與延遲是否穩定*

把 widget 切到「Stage 2 開環」，拖動到達率。

低於容量時：吞吐跟著漲、延遲平穩。

超過容量時：吞吐被削平（機器已滿）、延遲垂直上升。

紅色虛線就是 SLO 容量。

ρ = 到達率 / 服務率是利用率。M/M/1 的等待時間 ∝ 1/(1−ρ)，
所以 ρ = 0.9 時延遲是空載的 10 倍，ρ = 0.95 時是 20 倍。
容量規劃要留 20–30% 餘裕的理由就在這。

來源：[vllm bench serve CLI 文件](https://docs.vllm.ai/en/latest/cli/bench/serve/)、[vLLM goodput metric（PR #9338）](https://github.com/vllm-project/vllm/pull/9338)

### 77. SLO 邊界搜尋
*粗掃找範圍，細掃逼近，再重複確認*

常見錯誤：

① 只跑一次就下結論，而邊界點的變異最大。

② 用平均值取代 p95，邊界會被高估。

③ 量測窗太短，系統還沒進入穩定態就記錄。

④ 每個點之間沒有清空 prefix cache，導致後面的點虛胖。

來源：[vllm bench serve CLI 文件](https://docs.vllm.ai/en/latest/cli/bench/serve/)、[vLLM goodput metric（PR #9338）](https://github.com/vllm-project/vllm/pull/9338)

### 78. 從 RPS 換算服務人數
*Little's Law 與它的四個前提*

換算鏈條要一步一步講：

① 從 Stage 2 得到 SLO 容量（req/s）。注意用的是 goodput，不是 throughput。

② 每人每小時發幾次請求 → 每人的到達率。

③ 人數 = 系統到達率 ÷ 每人到達率。

Little's Law（L = λW）在這裡的用途是驗算：算出來的 L
應該小於等於你設定的 max_num_seqs。如果 L 比 max_num_seqs 大很多，
代表設定不一致，數字有問題。

四個前提：系統穩定、輸出長度分布與量測窗一致、SLO 在該吞吐下滿足、
使用者行為模型（每小時輪數）有依據。任何一個不成立，人數就不能引用。

來源：[vllm bench serve CLI 文件](https://docs.vllm.ai/en/latest/cli/bench/serve/)

### 79. 報告該寫什麼
*一份可以被別人重跑的容量報告*

這一頁可以直接拿去當報告模板的目錄。

特別提醒 revision hash：HuggingFace 上的模型會更新，
不記錄 revision 的話，三個月後重跑可能得到不同結果卻找不到原因。
本報告用的三個 revision 都寫在附錄 F。

來源：[vllm bench serve CLI 文件](https://docs.vllm.ai/en/latest/cli/bench/serve/)、[vLLM goodput metric（PR #9338）](https://github.com/vllm-project/vllm/pull/9338)、[Inside vLLM: Anatomy of a High-Throughput LLM Inference System](https://blog.vllm.ai/2025/09/05/anatomy-of-vllm.html)

## CH10 結論

### 80. 結論

最後 5 分鐘。若時間不夠，直接跳到「十個結論」那一頁唸完即可。

### 81. 十個結論
*把整份報告壓成十句話*

如果只能記住三句：

① decode 是 memory-bound，N* ≈ 292。

② 混合模型要分開算 KV 與 SSM，短對話由 SSM 主導。

③ speculative decoding 看 B×(k+1) 有沒有超過 N*。

### 82. 該轉哪個旋鈕
*從症狀出發的決策圖*

這張圖可以印出來貼在牆上。

第四支要特別講：preempt 是容量訊號，不是效能訊號。
看到 preempt 就別再想「怎麼調快」，該想的是「是不是不該收這麼多請求」。

### 83. 下一步：這份報告沒有回答的問題
*誠實列出邊界*

把邊界寫清楚，報告才站得住。

那個「1.7–3 倍」的說法要說清楚是經驗值：roofline 只算權重讀取與張量運算，
不含 attention kernel、MoE routing、all-to-all、取樣、Python 排程與 CUDA graph 之外的開銷。
一旦量過自己的比例，這個模型就變成很好用的快速估算工具。

## 附錄

### 84. 附錄

（無）

### 85. 附錄 A：名詞表（1/2）
*Acceptance length (τ) – HBM　·　共 41 條；隨時按 G 也能打開*

這一頁與 G 鍵的名詞表是同一份資料，由同一個字典產生。

### 86. 附錄 A：名詞表（2/2）
*ITL – vLLM　·　共 41 條；隨時按 G 也能打開*

這一頁與 G 鍵的名詞表是同一份資料，由同一個字典產生。

### 87. 附錄 B：config 與 checkpoint 的原始證據
*所有架構數字的來源*

驗證方式：把 config 的維度乘出來，逐一對應 checkpoint 的 tensor 名稱，
再與 HF API 回報的 dtype 元素數比對。三個模型的誤差分別是
0.009%、
0.018%、
0.000%。

NVFP4 的驗算最漂亮：48 × 256 × 3 × 3072 × 1024 = 115,964,116,992 個 4-bit 權重，
除以 2 得到 57,982,058,496 個 U8，與統計完全相同；再除以 group_size 16
得到 7,247,757,312 個 FP8 block scale，也完全相同。

來源：[Qwen3.5-122B-A10B config.json](https://huggingface.co/Qwen/Qwen3.5-122B-A10B/blob/main/config.json)、[Qwen3.8-27B config.json](https://huggingface.co/Qwen/Qwen3.8-27B/blob/main/config.json)、[NVFP4 safetensors index](https://huggingface.co/nvidia/Qwen3.5-122B-A10B-NVFP4/blob/main/model.safetensors.index.json)、[Qwen3.8-27B-FP8 safetensors index](https://huggingface.co/Qwen/Qwen3.8-27B-FP8/blob/main/model.safetensors.index.json)、[NVFP4 hf_quant_config.json](https://huggingface.co/nvidia/Qwen3.5-122B-A10B-NVFP4/blob/main/hf_quant_config.json)

### 88. 附錄 C：可以直接複製的命令
*服務啟動與壓測*

兩個對照組是必要的：

① 開 spec / 關 spec，其餘完全相同。

② 不同 k（例如 27B 的 k=7 與 k=3）。

沒有對照組就無法歸因。

來源：[vllm bench serve CLI 文件](https://docs.vllm.ai/en/latest/cli/bench/serve/)、[nvidia/Qwen3.5-122B-A10B-NVFP4 模型卡](https://huggingface.co/nvidia/Qwen3.5-122B-A10B-NVFP4)、[incoai/Qwen3.8-27B-DFlash2 模型卡](https://huggingface.co/incoai/Qwen3.8-27B-DFlash2)

### 89. 附錄 D：GDN 的數學細節
*單一 head 的更新式*

如果聽眾問「為什麼不用 Mamba2 就好」：Mamba2 只有整體衰減，
沒辦法針對某個特定的 key 精準改寫。在需要「更新某個事實」的任務上（例如多輪對話裡改口），
delta rule 那一項很重要。

來源：[Gated Delta Networks (arXiv 2412.06464)](https://arxiv.org/abs/2412.06464)、[vLLM Qwen GDN linear attention 實作](https://github.com/vllm-project/vllm/blob/main/vllm/model_executor/layers/mamba/gdn/qwen_gdn_linear_attn.py)、[vLLM mamba_utils.py（state shape 計算）](https://github.com/vllm-project/vllm/blob/main/vllm/model_executor/layers/mamba/mamba_utils.py)

### 90. 附錄 E：單卡與多卡
*TP / EP / DP 各自改變什麼*

補充 TP 對 KV cache 的限制：TP 會切 KV head，
但 122B 只有 2 組 KV head，所以 TP > 2 時 KV head 不夠切，
vLLM 會複製它們，每張卡的 KV cache 不會再變小。
TP=8 對 122B 因此特別不划算。

DP 的限制：每張卡都要放得下整份模型與足夠的 cache。
122B NVFP4 是 77.74 GiB，留給 cache 約
60 GiB，
所以單卡副本適合中等 context（≤ 64k）的高併發場景；
需要 256k context 時還是得用 TP 把 cache 空間合起來。

來源：[vLLM Architecture Overview](https://docs.vllm.ai/en/latest/design/arch_overview/)、[nvidia/Qwen3.5-122B-A10B-NVFP4 模型卡](https://huggingface.co/nvidia/Qwen3.5-122B-A10B-NVFP4)

### 91. 附錄 F：來源與資料界線
*查核日期 2026-09-08*

資料界線是這份報告最重要的自我約束。查核日期 2026-09-08；
HuggingFace 上的模型會更新，引用時務必連同 revision 一起寫。
