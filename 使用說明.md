# LLM 推論與服務容量

這個資料夾有三份文件：

| 檔案 | 說明 |
| --- | --- |
| **`LLM推論報告_v2.html`** | **第二版（建議使用）**。淺色主題、94 頁、40 張自製動畫 SVG、7 個互動試算器。數字都對齊實際要上線的量化版本。 |
| `LLM推論報告_v2.pdf` | 第二版的靜態版（94 頁）。動畫停在最終說明狀態，互動試算停在預設值。 |
| **`LLM推論技術部落格.html`** | 長文版。同樣的內容寫成可捲動的技術文章，34 張動畫 + 6 個試算器 + 31 筆行內引用。想快速讀懂就開這個。 |
| `講者筆記_v2.md` | 第二版每頁的講稿重點與來源連結。 |
| `LLM推論報告.html` / `.pdf` | 第一版，保留供對照。 |

## 開啟

用 Chrome 或 Edge 開啟 `LLM推論報告_v2.html`。所有樣式、腳本、字型堆疊、PNG 與 GIF
都已內嵌，完全離線可用，不需要伺服器或外部 CDN（QA 已驗證：載入過程零外部請求）。
只有點選頁尾的來源連結需要網路。

### 操作

| 按鍵 | 功能 |
| --- | --- |
| <kbd>→</kbd> <kbd>←</kbd> / <kbd>PgDn</kbd> <kbd>PgUp</kbd> | 換頁 |
| <kbd>Shift</kbd> + <kbd>→</kbd> | 跳到下一章 |
| <kbd>Space</kbd> | 本頁動畫下一步；跑完才換頁（<kbd>Shift</kbd>+<kbd>Space</kbd> 退一步） |
| <kbd>A</kbd> / <kbd>R</kbd> | 自動播放 / 重播本頁動畫 |
| <kbd>N</kbd> | 講者筆記（含本頁來源連結） |
| <kbd>O</kbd> | 目錄（可點選跳頁） |
| <kbd>G</kbd> | 名詞速查表（47 條） |
| <kbd>F</kbd> / <kbd>Esc</kbd> | 全螢幕 / 關閉 |
| <kbd>Home</kbd> <kbd>End</kbd> | 第一頁 / 最後一頁 |
| <kbd>?</kbd> | 操作說明 |

網址尾端加 `#54` 可直接開到第 54 頁。內文中虛線底的粗體字是專有名詞，滑鼠移過會出現解釋。

## 內容

10 章 + 6 篇附錄，共 94 頁。建議 90 分鐘；壓縮到 50 分鐘可保留 CH1（快帶）、CH4、CH6、CH7、CH9。

| 章 | 主題 | 起始頁 |
| --- | --- | --- |
| CH1 | 一個 token 是怎麼生出來的 | 4 |
| CH2 | vLLM 把它變成一個服務 | 17 |
| CH3 | 三個模型的架構帳本 | 26 |
| CH4 | Gated DeltaNet：用固定狀態換掉 KV cache | 33 |
| CH5 | 量化：NVFP4 與 FP8 | 42 |
| CH6 | 單卡記憶體帳本 | 48 |
| CH7 | Speculative decoding 對 serving 的影響 | 56 |
| CH8 | 延遲指標的定義與陷阱 | 69 |
| CH9 | 兩階段負載測試 | 76 |
| CH10 | 結論 | 83 |
| 附錄 | 名詞表、config 證據、命令、GDN 數學、TP/EP/DP、來源 | 87 |

### 互動試算器（7 個）

- **第 53 頁** B200 單卡記憶體帳本：切模型 / KV 精度 / spec 開關，拉 context 與併發數
- **第 54 頁** KV cache 與 SSM state 的成長比較
- **第 64 頁** speculative decoding 划不划算：調 k、α、併發數
- **第 74 頁** 高併發模擬：拉人數與 max_num_seqs，看 TTFT / ITL / 吞吐怎麼變
- **第 78 頁** Stage 1 閉環飽和測試
- **第 79 頁** Stage 2 開環 SLO 容量測試
- **第 81 頁** RPS → 服務人數（Little's Law）

## 這一版對齊的模型版本

| 用途 | Repo | 量化 | Speculative decoding |
| --- | --- | --- | --- |
| 大模型 | `nvidia/Qwen3.5-122B-A10B-NVFP4` | NVFP4（W4A4）+ FP8 KV | 內建 MTP，k = 3 |
| 中模型 | `Qwen/Qwen3.8-27B-FP8` | FP8 blockwise-128 | `incoai/Qwen3.8-27B-DFlash2`，k = 7（block size 8） |
| 對照 | `Qwen/Qwen3-32B` | BF16 | 無（checkpoint 沒有 MTP） |

所有架構數字與權重大小都由官方 config 逐張量推導，並與 HuggingFace 上 safetensors 的
dtype 統計交叉驗證，誤差 < 0.02%。checkpoint 的 revision hash 列在附錄 F。

## 數據界線（重要）

報告裡的數字分三級，每一頁都有標示：

| 等級 | 涵蓋哪些 | 能不能引用 |
| --- | --- | --- |
| 實測 / 官方 | 架構參數、checkpoint 位元組數、量化設定、NVIDIA 的 NVFP4 精度評測、DFlash2 模型卡的接受長度與加速倍數、vLLM issue 的實測數字、B200 規格 | 可以直接引用 |
| 公式推導 | KV / SSM cache 大小、記憶體帳本、roofline 轉折點、speculative decoding 的加速估計 | 公式都印在頁面上，可以自行驗算。標示為「解析上限」的，實測通常是它的 30–60% |
| 教學示例 | CH9 的吞吐 / 延遲 / 佇列曲線與 SLO 掃描圖，以排隊理論生成 | **不是任何硬體的實測結果**，不可當成 B200 實測成績對外引用 |

正式的容量報告要換成自家實測資料，並依附錄 C 的清單記錄引擎版本、workload、
量測窗與 SLO 定義。

## 修改與再產生

```bash
python v2/build.py          # 重新產生 HTML 與講者筆記
node v2/qa.cjs --pdf        # 逐頁截圖 + 版面溢出檢查 + 輸出 PDF
node v2/interact.cjs        # 29 項互動與數值一致性測試
```

| 檔案 | 內容 |
| --- | --- |
| `v2/model.py` | 解析模型：參數量、cache 公式、roofline、speculative decoding。所有數字的單一來源 |
| `v2/svgkit.py` | 極小的 SVG 產生器 |
| `v2/figs_core.py` `figs_arch.py` `figs_spec.py` `figs_load.py` `figs_new.py` | 40 張動畫 SVG |
| `v2/slides_part1..4.py` | 投影片內容、講稿與來源標註 |
| `v2/common.py` | 來源清單、名詞表、版面工具 |
| `v2/template.html` | 淺色主題 CSS 與播放引擎（1440×810 固定畫布，等比縮放） |
| `v2/widgets.js` | 7 個互動試算器 |
| `v2/assemble.py` | 組裝 HTML 與講者筆記 |
| `research/` | 抓下來的官方 config、safetensors 索引與 metadata |
| `v2/humanize*.py` | 文字修潤的改寫表（一次性腳本，已套用） |
| `qa2/` | 逐頁截圖、版面檢查與互動測試結果 |

改完跑 `python v2/build.py` 就好。投影片上的數字全部由 `v2/model.py` 推導，
沒有手打的常數；改架構參數會一起更新內文、表格、圖與互動試算器。

也可以用簡報下方的「列印 / PDF」自行輸出：選擇儲存為 PDF、紙張用「CSS 紙張尺寸」、
開啟背景圖形、關閉頁首頁尾。輸出前簡報會自動把所有動畫停在最終狀態。
